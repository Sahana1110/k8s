apiVersion: v1
kind: Service
metadata:
  name: nexus-service
spec:
  selector:
    app: nexus
  ports:
    - protocol: TCP
      name: ui
      port: 8081
      targetPort: 8081
    - protocol: TCP
      name: docker
      port: 5000
      targetPort: 5000
  type: NodePort

